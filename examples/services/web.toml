name = "web"
command = "/opt/odin/bin/my-web-app"
args = ["--host", "127.0.0.1", "--port", "8080"]
cwd = "/opt/odin/my-web-app"
autostart = true

env = { RUST_LOG = "info" }

user = "www"
group = "www"

restart = "on-failure"
restart_initial_delay = "1s"
restart_max_delay = "30s"

stdout_log = "/var/log/odin/web.out.log"
stderr_log = "/var/log/odin/web.err.log"

[healthcheck]
type = "http"
url = "http://127.0.0.1:8080/health"
interval = "10s"
timeout = "2s"
retries = 3
action = "restart"
